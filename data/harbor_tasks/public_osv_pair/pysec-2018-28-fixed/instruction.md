Read `/app/case.json` and write `/app/prediction.json` with exactly these fields:
`case_id`, `verdict`, and `reason_codes`. Determine whether the pinned package version is
covered by the frozen public OSV ecosystem range. Do not use the network.
