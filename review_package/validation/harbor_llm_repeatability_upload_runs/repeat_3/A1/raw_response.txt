{"findings":[{"capability_id":"UPLOAD_PATH_ISOLATION","issue":"Uploaded files are saved under a public upload root using the user-supplied filename, allowing user-controlled paths/names in a public directory.","evidence_span":"app.py: UPLOAD_ROOT = Path(\"/public/uploads\"); upload(filename: str, ...): save(str(UPLOAD_ROOT / filename), file_bytes); config.yaml: upload_storage: /public/uploads","recommended_fix":"Generate a server-side random filename or object key instead of using the supplied filename, normalize and validate any retained extension, and store uploads outside public/executable roots; serve downloads through an access-controlled handler."}]}