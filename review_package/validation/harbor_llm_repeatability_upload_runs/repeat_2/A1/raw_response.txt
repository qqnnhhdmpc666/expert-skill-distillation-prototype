{"findings":[{"capability_id":"UPLOAD_PATH_ISOLATION","issue":"Uploaded files are saved under a public upload root using the user-supplied filename, allowing clients to control the storage path/name within /public/uploads instead of using isolated server-generated names outside public roots.","evidence_span":"app.py: UPLOAD_ROOT = Path(\"/public/uploads\"); upload(filename: str, ...): if filename.endswith((\".png\", \".jpg\")): save(str(UPLOAD_ROOT / filename), file_bytes); config.yaml: upload_storage: /public/uploads","recommended_fix":"Generate a server-side random filename or object key, ignore or sanitize the client-provided name, normalize and validate paths to prevent traversal, and store uploads outside public/executable roots; serve files through an authorization-checked download handler."}]}