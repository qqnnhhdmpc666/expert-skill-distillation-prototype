Task: Independently authored holdout: clean config review.

Snippet:
prod.yml: audit.enabled=true, retention_days=90, export_sink='siem'. dev.api_token appears only under dev-only profile.

Requested output schema: capability_id, evidence_span, recommended_fix