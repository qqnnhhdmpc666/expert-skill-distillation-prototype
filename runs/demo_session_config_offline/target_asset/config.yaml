# config_audit 目标资产包

```yaml
env: production
debug: true
SECRET_KEY: changeme
jwt_secret: default
cors: "*"
audit_log_retention_days: null
error_template: "stacktrace=${stacktrace}, api_key=${api_key}"
```
